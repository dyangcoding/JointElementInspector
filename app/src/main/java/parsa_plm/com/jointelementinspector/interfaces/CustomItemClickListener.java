package parsa_plm.com.jointelementinspector.interfaces;

import android.view.View;

/*
    provide a common ability so that items will be clickable
 */
public interface CustomItemClickListener {
    public void onItemClick(View v, int position);
}
